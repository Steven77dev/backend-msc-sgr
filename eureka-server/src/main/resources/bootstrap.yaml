

spring:
  application:
  name: eureka-server
  cloud:
    config:
      enabled: true
      uri: http://localhost:8001
server:
  port: 8761

eureka:
  client:
    fetch-registry: false
    register-with-eureka: false

